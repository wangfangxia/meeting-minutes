# Word 目录与发言索引制作规范

## 文档与排版

- 输出为合法 OOXML `.docx` 包，正文使用可编辑段落和表格。选择可用的文档库或 Word 自动化，不依赖特定云服务。
- 中文正文采用常见可读字体、统一字号及段间距；长表重复表头，避免表格超出页边距；标题尽量与下段同页。复杂轨迹可用可编辑表格表达。
- 纪要的正文章节应用真正的 Heading 1/2/3 样式及正确大纲级别。封面信息、高频榜单、目录标题放在目录前，不进入正文目录。

## 论文式目录

- 插入 Word TOC 域，域指令为 `TOC \o "1-3" \h \z \u`，使用 begin/instrText/separate/end 域结构，不能只写普通“目录”列表。
- 文档设置加入 `w:updateFields w:val="true"`。这是请求更新，不保证所有阅读器自动执行。
- 能使用 Word 或兼容排版引擎时，更新全部域、目录和页码再保存，核验结果。不要为更新域结束用户已有 Word 进程。
- 无排版引擎时，在域的缓存结果中提供有实际标题文字的书签超链接目录，保证初次查看有导航；不填猜测页码。保留 TOC 域，交付说明“在桌面 Word 中右键目录→更新域→更新整个目录，可生成/刷新页码”。
- 页码须来自排版引擎。修改文档后目录页码可过期，原生域应可再次更新。

## 内部书签与双向链接

- 为标题、发言段落、发言人索引建立书签。使用唯一、ASCII、字母开头、不含空格、长度小于 40 的名称，如 `sec_001`、`utt_000001`、`spk_001`。
- 每个 `w:bookmarkStart` 有唯一数字 ID，并有相同 ID 的 `w:bookmarkEnd`；标签放在目标段落中，避免跨单元格配对。
- 内链使用 `w:hyperlink w:anchor="utt_000001"` 等方式，目标在同一文件内，显示文字用 Hyperlink 样式。不要为内部跳转创建外部 relationship。
- 正文发言段落有 `utt_...` 书签；段内姓名链接到该人的 `spk_...` 索引小节。索引每条链接回对应 `utt_...`。发言摘要与姓名映射不可代替核对原文。
- Word 的 Ctrl+F 提供文字搜索，时间点用来人工定位音频；不承诺点击文字直接播放音频。Word 设置不同可能需 Ctrl+单击超链接。

## 交付前检查

1. 两个 ZIP/OOXML 包可读，主要 XML 合法，无损坏文件。
2. 纪要包含 TOC 域、正确标题层级和域更新设置；书签名称及 ID 唯一且起止匹配。
3. 两份文档的每个内部 anchor 都能找到目标。转写稿每个正文发言均有索引条目，索引归属与正文发言人一致。
4. 对照底稿检查录音覆盖范围、时间点、完整发言顺序、未识别段落，以及跨录音的来源标签。单纯 XML 合法不能证明内容准确。
5. 能使用排版/预览工具时检查目录、长表、页边距及链接跳转；不能时如实说明只做了结构检查，不声称通过视觉或点击测试。
6. 跟进草稿和待确认清单均在纪要内；只交付纪要和转写稿两个 Word 文件。
